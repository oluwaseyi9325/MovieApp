import { StyleSheet } from "react-native";
// import Size from "../../utilities/useResponsiveness";

export const styles = StyleSheet.create({
  screen: {
    flex: 1,
    // paddingHorizontal: Size.calcWidth(20),
    backgroundColor: "white",
  },
  view: {
    flex: 1,
  },
});
